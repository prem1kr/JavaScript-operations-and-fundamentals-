function bodyMass(weight, height){
    let bodyMass=(weight)/height*height;
    return bodyMass;
}
 let weight=80;
 height=5.8;
let BMI= bodyMass=(weight)/height*height;
console.log(`The body mass index: ${BMI}`);
